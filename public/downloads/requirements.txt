requests>=2.31
pydirectinput>=1.0.4
pyautogui>=0.9.54
pywin32>=306 ; platform_system == "Windows"
psutil>=5.9
Pillow>=10.0
# Voice relay (real-time whisper reception via microphone).
# Already bundled inside BakersWhisper.exe — only needed when running from source.
SpeechRecognition>=3.10.0
PyAudio>=0.2.14 ; platform_system == "Windows"
