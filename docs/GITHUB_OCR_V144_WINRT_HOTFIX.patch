diff --git a/.github/workflows/build-windows.yml b/.github/workflows/build-windows.yml
index f531944..5d55ce1 100644
--- a/.github/workflows/build-windows.yml
+++ b/.github/workflows/build-windows.yml
@@ -38,15 +38,14 @@ jobs:
           pip install "SpeechRecognition>=3.10.0" "PyAudio>=0.2.14"
           # Loopback capture (WASAPI) deps — required for voice without mic.
           pip install "soundcard>=0.4.2" "numpy>=1.24" "mss>=9.0"
-          # OCR dep — best effort: if it ever fails, the exe still builds and
-          # OCR simply stays disabled at runtime (HAS_WINOCR guard).
-          pip install "winocr>=0.0.15" || echo "winocr unavailable - OCR will be disabled in this build"
+          # OCR is required; pin the API validated by the bridge.
+          pip install "winocr==0.0.15"
           pip install pywin32-ctypes
           python -c "import win32gui, win32con, win32process, win32api; print('pywin32 OK')"
           python -c "import psutil, requests, pyautogui, pydirectinput; print('deps OK')"
           python -c "import speech_recognition, pyaudio; print('voice deps OK')"
           python -c "import soundcard, numpy, mss; print('loopback deps OK')"
-          python -c "import winocr; print('OCR deps OK')" || echo "OCR deps missing - continuing without OCR"
+          python -c "import winocr; assert callable(getattr(winocr, 'recognize_pil_sync', None)); assert callable(getattr(winocr, 'recognize_pil', None)); print('OCR API OK')"
 
       - name: Inject build constants
         shell: pwsh
@@ -82,7 +81,7 @@ jobs:
       - name: Sanity-check the injected source
         shell: pwsh
         run: |
-          python -c "import ast; ast.parse(open('public/downloads/wim_bridge_gui.py', encoding='utf-8').read()); print('Python syntax OK')"
+          python -c "import ast; from pathlib import Path; files=['public/downloads/wim_bridge_gui.py','public/downloads/ocr_bridge.py']; sources=[Path(f).read_text(encoding='utf-8') for f in files]; [ast.parse(source) for source in sources]; assert all('asyncio.run(call)' not in source for source in sources); assert sum(source.count('async def _await_operation') for source in sources) >= 2; print('Python syntax + WinRT awaitable guard OK')"
 
       - name: Build with PyInstaller
         shell: pwsh
@@ -104,15 +103,11 @@ jobs:
             "--hidden-import", "win32process",
             "--hidden-import", "pywintypes"
           )
-          # Only bundle winocr when it actually installed, so the build never
-          # fails because of the OCR dependency.
-          python -c "import winocr" 2>$null
-          if ($LASTEXITCODE -eq 0) {
-            $pyargs += @("--collect-all", "winocr", "--hidden-import", "winocr")
-            Write-Host "winocr detected - bundling OCR"
-          } else {
-            Write-Host "winocr NOT detected - building without OCR"
-          }
+          $pyargs += @(
+            "--collect-all", "winocr",
+            "--hidden-import", "winocr",
+            "--hidden-import", "ocr_bridge"
+          )
           $pyargs += "public/downloads/wim_bridge_gui.py"
           pyinstaller @pyargs
 
diff --git a/public/downloads/ocr_bridge.py b/public/downloads/ocr_bridge.py
index 989caf8..c9562ad 100644
--- a/public/downloads/ocr_bridge.py
+++ b/public/downloads/ocr_bridge.py
@@ -153,33 +153,37 @@ def recognize_image(image, language: str = "pt") -> str:
     if not OCR_AVAILABLE:
         raise RuntimeError("OCR indisponível: winocr/Pillow/pywin32 não foram empacotados")
     prepared = preprocess(image)
+    last_error = None
     sync = getattr(winocr, "recognize_pil_sync", None)
     if callable(sync):
-        last_error = None
         for lang in (language, "en", None):
             try:
                 result: Any = sync(prepared, lang) if lang else sync(prepared)
                 if isinstance(result, dict):
                     return str(result.get("text", ""))
                 return str(getattr(result, "text", result or ""))
-            except (TypeError, ValueError, RuntimeError) as error:
+            except Exception as error:
                 last_error = error
-        if last_error:
-            raise last_error
     async_recognize = getattr(winocr, "recognize_pil", None)
     if callable(async_recognize):
         import asyncio
-        last_error = None
         for lang in (language, "en", None):
             try:
-                call = async_recognize(prepared, lang) if lang else async_recognize(prepared)
-                result = asyncio.run(call)
+                async def _await_operation():
+                    operation = (
+                        async_recognize(prepared, lang)
+                        if lang
+                        else async_recognize(prepared)
+                    )
+                    return await operation
+
+                result = asyncio.run(_await_operation())
+                if isinstance(result, dict):
+                    return str(result.get("text", ""))
                 return str(getattr(result, "text", result or ""))
-            except (TypeError, ValueError, RuntimeError) as error:
+            except Exception as error:
                 last_error = error
-        if last_error:
-            raise last_error
     raise RuntimeError(
         "winocr incompatível: esperado recognize_pil_sync ou recognize_pil; "
-        f"versão instalada expõe: {', '.join(x for x in dir(winocr) if x.startswith('recognize'))}"
+        f"versão instalada expõe: {', '.join(x for x in dir(winocr) if x.startswith('recognize'))}; último erro: {last_error}"
     )
diff --git a/public/downloads/wim_bridge_gui.py b/public/downloads/wim_bridge_gui.py
index 2abfdf0..793c409 100644
--- a/public/downloads/wim_bridge_gui.py
+++ b/public/downloads/wim_bridge_gui.py
@@ -24,7 +24,7 @@ from __future__ import annotations
 API_URL = "https://wimmsg-lntm.vercel.app"
 BRIDGE_TOKEN = "REPLACE_WITH_YOUR_TOKEN"
 APP_NAME = "Bakers Whisper"
-APP_VERSION = "1.4.3"
+APP_VERSION = "1.4.4"
 # =============================================================================
 
 import asyncio
@@ -131,8 +131,18 @@ def ocr_recognize(image, language: str = "en") -> str:
     if callable(async_recognize):
         for lang in langs:
             try:
-                call = async_recognize(image, lang) if lang else async_recognize(image)
-                return _text(asyncio.run(call))
+                # winocr returns a WinRT IAsyncOperation. It is awaitable, but
+                # asyncio.run() only accepts a native coroutine object. Create
+                # the operation inside a tiny coroutine and await it there.
+                async def _await_operation():
+                    operation = (
+                        async_recognize(image, lang)
+                        if lang
+                        else async_recognize(image)
+                    )
+                    return await operation
+
+                return _text(asyncio.run(_await_operation()))
             except Exception as error:
                 last_error = error
 
